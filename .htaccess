Options +FollowSymLinks
RewriteEngine On

# Allow direct access to assets, admin, includes, uploads, telegram_bot
RewriteRule ^(admin|assets|includes|uploads|telegram_bot)($|/) - [L]

# Route slug or numeric id to post.php
RewriteRule ^([A-Za-z0-9\-]+)/?$ post.php?slug_or_id=$1 [L]
